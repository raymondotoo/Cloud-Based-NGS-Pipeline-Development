This folder contains legacy `*_fixed.Rmd` notebook variants and related reference files.

They are retained for provenance only.

The canonical workflow lives in the top-level original notebooks and uses `project_setup.R`.
